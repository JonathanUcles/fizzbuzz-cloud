import React from 'react'
import { render } from '@testing-library/react'
import Form from './Form'
describe('Mount Form', () => { 
    test('Render Form in React',()=>{
        render(<Form />)
    })
 })